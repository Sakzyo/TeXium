TeXium 1.0.0 fixes excess vertical space in the right inspector. Empty Outline, Problems, References, History, Notes, and clean Git views now show their guidance at the top, and the Git summary no longer reserves an oversized area above the change list.

The DMG contains a Developer ID signed and notarized universal arm64/x86_64 app for macOS 14 or later, plus an Applications shortcut.

Validation includes the 73-test core suite, the focused inspector UI test, strict code-signature verification, notarization and stapling checks for both the app and DMG, a mounted-image content check, and Gatekeeper assessment.
